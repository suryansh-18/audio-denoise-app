# 🎧 Audio Denoise App (Streamlit)

A simple Streamlit web app for removing noise from audio files.  
Supports **WAV, MP3, MP4, FLAC, OGG** and allows:

- Automatic noise removal (aggressive or normal)
- Manual cleaning (lowpass, notch, rumble removal)
- Low-frequency restoration to preserve bass
- Audio preview + waveform + spectrogram
- MP3/MP4 → WAV conversion
- Export cleaned audio + PDF report

---

## 🚀 Run Locally

Install requirements:

```bash
pip install -r requirements.txt
